include<stdio.h>
#include<math.h>
int Count1(long b,int B);
int Count2(long a,int A);
int main()
{
	long a,b;
	int A,B;
	int PA,PB;
	scanf("%ld %d %ld %d",&a,&A,&b,&B);
	PA=Count1(a,A);
	PB=Count1(b,B);
	printf("%d\n",PA+PB);
	return 0;
}
int Count1(long a,int A)
{
	int i,j=0,c=0;
	for(i=1;i<10;i++)
	{
		if((a%(int)(pow(10,i))-a%(int)(pow(10,i-1)))/(int)(pow(10,i-1))==A)
		{
			c+=A*(int)(pow(10,j));
			j++;
		}
	}
	return c;
}